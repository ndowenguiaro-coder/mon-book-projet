// Laisser vide : quand ce site est servi par le backend FastAPI lui-même
// (cas par défaut, voir README), l'API et le site partagent la même URL —
// aucune configuration n'est nécessaire.
//
// Si un jour vous hébergez le site séparément du backend (ex. site sur
// Netlify, API sur un autre serveur), renseignez ici l'URL complète du
// backend, par exemple : 'https://api.votredomaine.com'
const API_BASE_URL = '';
