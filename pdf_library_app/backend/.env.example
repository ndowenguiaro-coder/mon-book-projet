# Copier ce fichier en ".env" (jamais commité dans Git) et renseigner de vraies valeurs.

# Clé secrète pour signer les jetons JWT. Générer une vraie valeur aléatoire, par exemple :
#   python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me-in-production
